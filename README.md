# 홍유택 — Global E-commerce MD · Operations Automation

**본업은 글로벌 채널 MD입니다. 반복 업무를 없애기 위해 코드를 배웠고,
지금은 운영의 상당 부분을 파이프라인이 대신합니다.**
한국·일본·대만·홍콩 4개국, 자사몰·마켓플레이스·오프라인을 아우르는 여러 판매 채널을
직접 운영하며, 그 운영 경험을 그대로 자동화의 요구사항으로 옮겨 왔습니다.

---

## 📌 Featured — 두 저장소를 순서대로 보시면 됩니다

### 1️⃣ [ai-orchestration-portfolio](https://github.com/hyt971023-sys/ai-orchestration-portfolio) — 무엇을 왜 자동화했고, 틀렸을 때 어떻게 대비했나
운영 자동화의 **설계·측정 방법론**입니다. 성숙도 프레임워크, 안전장치 우선 설계 원칙,
그리고 **실패 케이스 6건**(자체 안전장치가 유출을 담은 채 "전 항목 통과"를 출력하던 사고 포함)을
문제 → 접근 → 결과 → 증명 구조로 정리했습니다.

### 2️⃣ [multichannel-inventory-allocator](https://github.com/hyt971023-sys/multichannel-inventory-allocator) — 그래서 짜면 이렇게 나온다
실무에서 다뤄 온 문제 유형을 합성 데이터로 처음부터 재구현한 **클린룸 데모**입니다.
하나의 창고 재고를 여러 채널에 규칙 기반으로 배분하고 → 채널별 포맷(API/업로드 시트)으로 출력 →
append-only 이력 → 운영자 알림 다이제스트까지, 파이프라인 전체를 담았습니다.
*Python 표준 라이브러리만 사용 · 의존성 0 · 33개 단위 테스트 · CI 포함 · dry-run 기본값 · clone 후 즉시 실행.*

---

## 🛠 실무 프로젝트 하이라이트 (일반화 서술)

**1. 멀티채널 재고 배분·동기화 파이프라인** — Node.js
- 문제: 여러 채널이 같은 창고 재고를 나눠 쓰면서 수작업 배분 → 오버셀·품절 사고 위험.
- 설계: 채널별 배분율 정책 + 저재고 컷오프, 자사몰 API 자동 푸시 + 마켓플레이스 업로드 시트 생성,
  평일 정시 자동 실행(launchd) + 메신저 웹훅 알림, 실행 이력 append-only 저장.
- 결과: 국가별 재고 배분을 **평일 매일 무인 실행되는 루틴으로 전환해 수개월째 운영 중**,
  동일 구조를 두 번째 국가 채널로 이식.

**2. 마켓플레이스 모니터링·재고 자동화** — Python
- 시간별 매출 추적, 재고 임계치 알림(cron으로 매시간 상시 구동 중), API 기반 재고 자동 입력
  스크립트 일체를 구축해 일상 운영에 사용.
- 인증서 기반 API 클라이언트, "응답 Success ≠ 실제 반영"을 전제로 한 사후 검증 로직 포함.

**3. 사내 운영 도구 — 프로모션 기획 모듈 기획·설계**
- 행사 기획 업무를 화면으로 옮기는 모듈에서 **요구정의, 화면·데이터 구조 설계, 릴리스별 검수를
  직접 수행** (구현은 개발 리소스와 협업). 기획 산출물 자동화와 다국가 운영 지원까지
  반복 릴리스로 확장.

**4. 마켓플레이스 ↔ 풀필먼트(3PL) 연동**
- 주문·출고 API 연동을 등록·검증하고, 리스크를 줄이기 위해 수동 운영과 병행하는 단계적
  컷오버로 설계. 키 만료 주기를 캘린더로 관리해 무중단 갱신.

**5. 운영 안전 체계 (모든 자동화의 공통 원칙)**
- 라이브 변경 전 타임스탬프 백업 → 변경 → 검증 → 종료 시 원복. dry-run이 기본값, 라이브는 명시적.
- 장애·실수 사례를 회고 문서로 축적해 다음 자동화의 가드레일로 재사용.

---

## 🧰 Skills

- **Languages**: Python, JavaScript/Node.js, SQL(조회·집계)
- **Integration**: 커머스 플랫폼·마켓플레이스 REST API 연동 다수
  (Cafe24, Qoo10, Rakuten RMS, Amazon SP-API, Shopline, Shopee Open Platform 등)
- **Automation**: cron/launchd 스케줄링, 메신저 웹훅 알림, CSV/Excel 대량 처리 자동화
- **Ops**: 크로스보더 물류·정산 프로세스, 채널 정책(수수료·페널티) 분석, CS 운영 자동화
- **Domain**: K-beauty / 글로벌 이커머스 (일본·대만·홍콩 시장)

## 🧭 일하는 방식

- 두 번 반복된 업무는 자동화 후보로 본다. 세 번째는 파이프라인이 한다.
- 자동화보다 **안전장치가 먼저**다 — 백업 없는 라이브 변경은 하지 않는다.
- 모르면 추측하지 않고 공식 창구에 문의한다. 문의 문안까지가 실무다.

---

> ⚠️ **공개 기준** — 업종·직무·기술 스택은 공개하되, **사명·내부 지표·내부 코드·내부 시스템 상태는
> 쓰지 않습니다.** 이 페이지와 두 저장소의 모든 서술은 그 기준에 따라 일반화되어 있고,
> 데모는 전부 합성 데이터로 새로 만든 것입니다. 상세는 개별 요청 시 공유 가능합니다.

📫 Contact: `hyt971023@gmail.com`
📂 [ai-orchestration-portfolio](https://github.com/hyt971023-sys/ai-orchestration-portfolio) · [multichannel-inventory-allocator](https://github.com/hyt971023-sys/multichannel-inventory-allocator)
